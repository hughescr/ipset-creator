# /lib/systemd/system/ipset-creator.service
[Unit]
Description=IPSet Country/Continent List Importer
Documentation=https://github.com/hughescr/ipset-creator
Requires=network-online.target
After=network-online.target
[Service]
Type=oneshot
ExecStart=/usr/sbin/ipset list -n -q north-america >/dev/null && /usr/sbin/ipset restore -f <($(/usr/bin/yarn global bin)/ipset-creator -r) || /usr/sbin/ipset restore -f <($(/usr/bin/yarn global bin)/ipset-creator)
PrivateTmp=true
